const validateBody = require("./validateBody");
const isValidId = require("./isValidId");
const isExistContact = require("./isExistContact");

module.exports = { validateBody, isValidId, isExistContact };
